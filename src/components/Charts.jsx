import React from "react";
import ChartDashboard from "./ChartDashboard";

const Charts = ({ activeProjectId }) => {
  return <ChartDashboard activeProjectId={activeProjectId} />;
};

export default Charts;
